import { SafeAreaView } from "react-native";
import TextWithColor from "@/app/shared/components/TextWithColor";

export default function ProfesionalDetails() {
    return (
        <SafeAreaView>
            <TextWithColor>Añadir detalles para el profesional</TextWithColor>
        </SafeAreaView>
    )
}